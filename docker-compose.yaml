version: "3.3"

services:

  backend_app:
    ports:
      - "80:80"
    restart: on-failure
    build: ./


  postgres:
    image: postgres:14.1-alpine
    environment:
      POSTGRES_USER: "rizyfbtd"
      POSTGRES_PASSWORD: "mtgn_9cgEWDalyPDg3ka4utR0TfVvRF2"
      POSTGRES_DB: "rizyfbtd"
      POSTGRES_HOST: "mouse.db.elephantsql.com"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
      - .sql_scripts:/docker-entrypoint-initdb.d

volumes:
  db-data:
